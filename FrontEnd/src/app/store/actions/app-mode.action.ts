import { createAction } from '@ngrx/store';

export const UpdateAppModeAdmin = createAction('[AppMode] UPDATE ADMIN');
export const UpdateAppModeGeneral = createAction('[AppMode] UPDATE GENERAL');
